.env
# Edge Function secrets (pushed with `supabase secrets set --env-file`)
supabase/functions/.env.secrets
# Supabase CLI local link state
supabase/.temp/
# NOTE: migrations are tracked as of Phase 18, by explicit choice. They embed
# deployed vault secret values in plaintext (0002, 0003, 0005, 0011, 0013), and
# this repo is public — rotate those secrets rather than relying on them being
# unlisted here.

# Flutter/Dart
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# IDE
.idea/
*.iml
.DS_Store

# Category harness feed cache (`node tools/category_harness.mjs --cache`)
tools/.category_harness_cache.json

# Claude Code local state
.claude/scheduled_tasks.lock
